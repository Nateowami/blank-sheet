# blank-sheet

Primary/secondary translation-fusion experiment.

## Run

```bash
python run_experiment.py
```

This writes intermediary token-graph artifacts and merged outputs to `artifacts/fusion_run/`.

## Test

```bash
python -m unittest discover -s tests -p "test_*.py"
```
